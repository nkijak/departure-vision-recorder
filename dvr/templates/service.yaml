apiVersion: v1
kind: Service
metadata:
  name: {{ include "dvr.fullname" . }}
  labels:
    {{- include "dvr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dvr.selectorLabels" . | nindent 4 }}
